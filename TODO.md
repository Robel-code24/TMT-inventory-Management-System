# Hosting locally — TODO

## Step 1: Backend setup
- [ ] Create backend venv (if not present)
- [ ] Install backend dependencies (`pip install -r backend/requirements.txt`)
- [ ] Start backend using `backend/start.ps1` (or `uvicorn ...`)

## Step 2: Frontend setup
- [ ] Install frontend dependencies (`npm install`)
- [ ] Start frontend using `frontend/start.ps1` (or `npm run dev`)

## Step 3: Verify app
- [ ] Open `http://localhost:5173` in browser
- [ ] Login with demo account(s)

## Step 4: Optional sanity checks
- [ ] Confirm backend docs at `http://127.0.0.1:8000/docs`
- [ ] Confirm API health at `http://127.0.0.1:8000/health`

